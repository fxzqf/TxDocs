"use strict";
class Athon {
    static token() {
        const data = null;
        const xhr = new XMLHttpRequest();
        xhr.withCredentials = true;
        xhr.open("GET", "");
        xhr.send(data);
        return "";
    }
}
